#include <stdio.h>
int main(int argc, char const *argv[])
{
	float m;
	int o;
	do{
		printf("ingrese su moneda");scanf("%i",&m);
			s+=m;
			printf("otra moneda");scanf("%i",&o);

	}while(o==0);
	printf("total %f")
	return 0;
}