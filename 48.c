#include<stdio.h>
int main(int argc, char const *argv[])
{
	char s,e,e2,m,prom,h,prom2,p;
	do{
		printf("sexo y edad");scanf("%c %c",&s,&e);
	if (s==m){
		++m;
		++e;
		prom==e/m;
	}
	if (s==h){
		++h;
		++e2;
		prom2=e2/h;
	}
	++p;
}while(p=15);
	return 0;
}