#include <stdio.h>
int main(int argc, char const *argv[])
{
	int n,num1,smt;
	printf("dime un numero");scanf("%I",n);
	for (int x = 0; n < count; ++i)
	{
	scanf("%i",&num1);
	smt=smt*num1;
	}
	printf("%i",smt);
	return 0;
}