#include<stdio.h>
int a, b, c;
int main()
{
	int valor;
	printf("tres valores");
	scanf("%d %d %d\a", &a, &b, &c);
	valor=a+b+c;
	printf("valor de a: %d\n", a);
	printf("valor de a: %d\n", b);
	printf("valor de a: %d\n", c);
	printf("la suma de las variables: %d", valor);
	return 0;
}
